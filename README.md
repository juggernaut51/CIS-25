# CIS-25
CIS 25 last half of the course assignments

module 7 HW assigment
-First I use struct to create my item

after finishing the assigment I went back and added a random string generator to make the product name becuase orginaly I had the name and ID be the same, which was boring

I included a helper function to help sort the names becuase in the instructions you said have the arrary sorted by names and since im making random names I couldnt pre sort them

Next function is my binary search function, i pretty much just copied the youtube video, it sets low as the first variable and high as the last andd then mid is the in between, then it proceds to search from the mid point

in the main function I set the size to be 100 as instruted. I inlcude my random seed to make a new random for each instance. Then I have a for loop to fill the array with random names. I then use C++ sort function to actually sort the array by name. Then the id numbers are addded to the array with a for loop. 

Then the user is prompted to input an ID as instructed
the result is found and then outputted for the user.

and then memory is freed up as instructed.


